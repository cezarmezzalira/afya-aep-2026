void main() {
    // O código vai aqui
    /*
     * Cenário: Um sistema precisa registrar
     * se um usuário está ativo ou não,
     * e qual a categoria de acesso dele (A, B ou C).
     * Desafio: Declare uma variável do tipo boolean
     * chamada usuarioAtivo e inicie-a com true .
     * Declare uma variável char chamada
     * categoria e inicie-a com a letra 'A' .
     * Imprima as duas variáveis.
     * Objetivo: Apresentar o tipo booleano
     * na prática (já que ele aceita apenas
     * valores como verdadeiro ou falso )
     * e reforçar o uso de aspas simples para o char.
     */

    // declarar uma variável do tipo boolean 
    // chamada usuarioAtivo e inicializar 
    // com o valor true
    boolean usuarioAtivo = true;

    // declarar uma variável do tipo char
    // chamada categoria e inicializar com o valor 'A'
    char categoria = 'A';

    // imprimir o valor da variável usuarioAtivo
    System.out.println(usuarioAtivo);

    // imprimir o valor da variável categoria
    System.out.println(categoria);
}